Para aprovisionar las tarjetas de los libros, primero se tienen que cargar los atributos en el layout "Item_libro.xml" (Hay que modificar esa parte para que los atributos no se carguen en el momento que se inicia la aplicación).

Actualizar las pags de acuerdo a los datos en la base de datos (Pendiente)